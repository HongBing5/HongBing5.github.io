# 2026-09-11 工作日志

## 重启 Hexo 博客（面试准备 + 个人展示）
- 用户想重新捡起此前练习的前端 Hexo 博客，用于面试展示和技能强化。
- 项目事实（已核对）：
  - Hexo 6.3.0；主题 `redefine`（配置覆盖文件 `_config.redefine.yml`，非 theme 目录内改）。
  - npm 脚本：build=hexo generate、clean=hexo clean、deploy=hexo deploy、server=hexo server。
  - 文章目录 `source/_posts/`（含 Vue-day01~06、Clash 使用、Hexo主题修改等 22 篇）。
  - 独立页面：`source/about`、`tags`、`categories`、`links`、`masonry`、`archives`。
  - 部署：`hexo-deployer-git` → `github.com/HongBing5/HongBing5.github.io.git` 分支 `gh-pages`。
- 本回合：梳理基础操作（启动/建文/建页/生成/部署）。
- 修复：项目 node_modules 不完整，本地 `hexo` 核心缺失，导致 `hexo s` 报 "Cannot find module 'hexo'"。在 `D:\hexo\blog` 执行 `npm install`（装回 272 个包）后解决，`hexo -v` 正常输出 hexo 6.3.0。
- 非阻塞提示：redefine 主题 v2.8.2 有更新（v2.9.0），可后续按需升级，不影响运行。
- 页面与笔记修改待用户确认后推进。

## 导航栏精简（已执行）
- 用户决定保留 Tags、去掉 Categories（内容杂/随手记，标签更合适）。
- 编辑 `D:\hexo\blog\_config.redefine.yml` 的 `navbar.links` 段，将 Categories 三项注释掉。
- 改后导航：Home / Tags / 相册 / About(我/友链)。改主题配置需重启 `hexo s` 生效。

## 删除 6 篇 Vue 笔记（已执行）
- 用户判断这几篇价值低（AI 可替代、腾讯文档已备份），决定删除让博客更干净。
- 操作：先复制到 `D:\hexo\blog\backup_vue_notes\` 本地留底，再从 `source/_posts/` 删除 Vue-day01~06。
- 删除时系统 safe-delete 的"送回收站"步骤报 trash-failed，但源文件已移出博客；恢复渠道=本地 backup_vue_notes + 腾讯文档。
- 后续：用户重启 hexo s 即可看到干净博客。
